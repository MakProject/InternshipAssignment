server {
    listen 81;
    server_name localhost;

    location = /favicon.ico { access_log off; log_not_found off; }
    location /static/ {
        root /home/ubuntu/demo/InternshipAssignment/backend;
    }

    location / {
        include proxy_params;
        proxy_pass http://localhost:8000;
    }
}

